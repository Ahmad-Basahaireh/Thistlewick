# =====================================================
# Thistlewick ? application configuration
# =====================================================

# --- H2 Database (file mode: data persists between runs) ---
db.url=jdbc:h2:./data/thistlewick;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1
db.username=sa
db.password=

# --- H2 driver ---
db.driver=org.h2.Driver

# --- Application metadata ---
app.name=Thistlewick
app.version=1.0.0-SNAPSHOT